import ValueAllParameters from "./value-all-parameters";
export default function ValueAllParameter(
// validators : Validators,
// validation : (result:ListReturn<Validators>)=>ReturnType,
// message : (result:ListReturn<Validators>)=>MessageType,
{ validators, validation, message, }) {
    return ValueAllParameters(validators, validation, message);
}
//# sourceMappingURL=value-all-parameter.js.map