import ListCallbackParameters from "./list-callback-parameters";
export default class ListCallbackParameter extends ListCallbackParameters {
    constructor({ value, validator, map, validation, message, }) {
        super(value, validator, map, validation, message);
    }
}
//# sourceMappingURL=list-callback-parameter.js.map